<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="5dp">

    <androidx.cardview.widget.CardView
        xmlns:card_view="http://schemas.android.com/apk/res-auto"
        android:layout_width="35dp"
        android:layout_height="0dp"
        card_view:cardBackgroundColor="@color/purple_700"
        android:id="@+id/statusrowlayout_tile"
        android:clickable="false"
        android:focusable="false"
        android:focusableInTouchMode="false"
        card_view:cardCornerRadius="6dp"
        android:layout_alignTop="@id/statusrowlayout_msg"
        android:layout_alignBottom="@id/statusrowlayout_msg"
        >

        <TextView
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:text="i"
            android:textStyle="bold|italic"
            android:textSize="6pt"
            android:textColor="@color/white"
            android:gravity="center"
            />
    </androidx.cardview.widget.CardView>

    <TextView
        android:id="@+id/statusrowlayout_msg"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentTop="true"
        android:layout_alignParentEnd="true"
        android:layout_marginStart="10dp"
        android:layout_toEndOf="@id/statusrowlayout_tile"
        android:clickable="false"
        android:focusable="false"
        android:focusableInTouchMode="false"
        android:text="ITEM NAME "
        android:textColor="@android:color/black"
        android:minHeight="20dp"/>

</RelativeLayout>